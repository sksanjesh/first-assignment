The Program is wriiten in Java and requires jdk version 1.7 32 bits to Function.

To Run the main Program....


		RUN THE LOGIN FILE...
		THATS WHERE THE PROGRAM STARTS


The Program uses BookCompany Access database specially designed to cater for data storage of this system.

The default password is "passwordm' for managers and passwordc for clerks.

It stores all basic information the user required us to deliever in the first presentation.